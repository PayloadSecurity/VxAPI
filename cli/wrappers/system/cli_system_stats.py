from cli.wrappers.cli_caller import CliCaller


class CliSystemStats(CliCaller):

    help_description = 'Returns a package of webservice statistics, e.g. the total number of submissions, unique submissions, signature ID distribution, user comments etc. by \'{}\''

