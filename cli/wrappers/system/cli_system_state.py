from cli.wrappers.cli_caller import CliCaller


class CliSystemState(CliCaller):

    help_description = 'Returns a full system state query, including all available action scripts, environments, files in progress etc. by \'{}\''

